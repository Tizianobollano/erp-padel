// Nombre del club: dato de configuracion, no marca definida todavia (wiki/index.md, decision
// pendiente 2 -- "Nombre comercial y marca del producto"). Placeholder unico, centralizado aca
// para no hardcodear el string en dos JSX distintos (encabezado publico + AppShell del panel).
// Cuando el proyecto tenga club piloto, este valor pasa a ser dato real (env var o tabla D1),
// sin tocar los componentes que lo consumen.
export const CLUB_NOMBRE = "Club de Padel";
